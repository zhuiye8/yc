# 二批次接口需求清单2

更新时间：2026-06-09

本文档只记录当前前端仍需要后端确认或补齐的接口。已上线并正常接入的 ChainOrg / ChainTalent 基础接口不再重复列为需求。

## 1. 首页统一统计接口

应用场景：首页 5 个统计数字。

当前状态：
- 企业总数、人才总数、技术标准已使用 `GET /api/stats/industry-chain-total`。
- 金融产品、申报政策仍是前端静态值。

建议新增或扩展接口：

```http
GET /api/stats/home-overview
```

请求参数：
- `province`：选填，例如 `湖北`。
- `city`：选填，例如 `宜昌`。

返回示例：

```json
{
  "code": "0",
  "message": "OK",
  "data": {
    "enterpriseTotal": 2080945,
    "talentTotal": 4766250,
    "standardTotal": 409709,
    "financialProductTotal": 257,
    "policyTotal": 41
  }
}
```

要求：
- 5 个数字必须为同一统计时点、同一地区口径。
- 地区参数建议兼容 `湖北/湖北省`、`宜昌/宜昌市`。
- 企业、人才、技术标准是否仅统计 6 条一级产业链全国数据，需要后端明确口径。

## 2. 政策列表与政策统计接口

应用场景：政策菜单、首页申报政策统计。

当前状态：
- 政策菜单使用前端本地静态政策数据。
- 客户数据字典中存在 `industrial_policy`、`policy_keyword`、`policy_tag`。
- Swagger 当前未看到专门政策接口。

建议新增接口：

```http
GET /api/policies/search
GET /api/policies/count
```

列表请求参数：
- `keyword`：选填，标题/正文/标签模糊搜索。
- `region`：选填，地区。
- `level`：选填，政策层级。
- `tag`：选填，政策标签。
- `page`、`pageSize`：分页参数。

列表返回字段建议：
- `id`
- `title`
- `sourceName`
- `publishDate`
- `expireDate`
- `level`
- `tags`
- `summary`
- `url`

统计返回示例：

```json
{
  "code": "0",
  "message": "OK",
  "data": {
    "total": 41
  }
}
```

## 3. 金融产品列表与统计接口

应用场景：资金菜单、首页金融产品统计。

当前状态：
- 资金菜单使用前端静态 `fundingProducts`、`investmentInstitutions`。
- 数据字典中存在 `fund_project`，但它更像科研基金/项目数据，是否等于“金融产品”需要后端确认。

建议新增接口：

```http
GET /api/financial-products/search
GET /api/financial-products/count
```

列表请求参数：
- `keyword`：选填。
- `type`：选填，产品类型。
- `industry`：选填，适用产业。
- `region`：选填。
- `page`、`pageSize`：分页参数。

列表返回字段建议：
- `id`
- `name`
- `type`
- `institution`
- `maxAmount`
- `rate`
- `term`
- `applicable`
- `applyUrl`

统计返回示例：

```json
{
  "code": "0",
  "message": "OK",
  "data": {
    "total": 257
  }
}
```

## 4. 人才图谱关系增强

应用场景：人才菜单搜索专家后，关系图需要支持顶部筛选：
- 全部
- 专家合作
- 专家关联企业
- 专家关联技术
- 专家关联产业链根节点
- 专家所属产业链

当前状态：
- `GET /api/talents/{id}/graph` 已可用于基础关系图。
- 但技术节点、产业链节点、所属产业链等关系仍不够明确，前端只能做弱推断。

建议增强接口：

```http
GET /api/talents/{id}/graph
```

节点类型建议：
- `PERSON`
- `ORG`
- `ENTERPRISE`
- `TECH`
- `CHAIN_ROOT`
- `CHAIN`

关系类型建议：
- `COAUTHOR`
- `AFFILIATED`
- `COOPERATE`
- `RELATED_ENTERPRISE`
- `RELATED_TECH`
- `RELATED_CHAIN_ROOT`
- `BELONG_CHAIN`

节点字段建议：
- `id`
- `name`
- `type`
- `org`
- `region`
- `count`
- `weight`

要求：
- 关系类型必须稳定，便于前端筛选。
- 若 `ORG` 中包含高校、科研院所、企业，建议返回 `orgType` 或拆分为 `ENTERPRISE`。

## 5. 已接入但需保持稳定的接口

以下接口前端已经接入，请后端保持参数与返回结构稳定：

```http
GET /api/chain-orgs/search
GET /api/chain-orgs/province-distribution
GET /api/chain-orgs/city-distribution
GET /api/chain-orgs/coverage-summary
GET /api/chain-orgs/node-org-counts
GET /api/chain-talents/search
GET /api/chain-talents/province-distribution
GET /api/chain-talents/city-distribution
GET /api/chain-talents/year-trend
GET /api/stats/industry-chain-total
```

特别注意：
- `chain` 参数需要支持一级、二级、分组节点、叶子节点。
- 企业接口 `tags` 作为企业标签筛选。
- 人才接口地区筛选使用人才所在地区，不再使用籍贯。
- 所有列表接口的 `total` 必须与筛选条件一致，不能只返回当前页数量。

# 二批次接口需求清单1

## 1. 产业链节点企业列表增加企业类型筛选

场景：产业图谱节点右键打开“相关企业”、链上企业柱状图点击打开企业列表时，需要按企业类型筛选。

接口范围：
- 节点企业列表接口
- 产业链企业汇总列表接口
- WF 企业搜索兜底接口

需求：
- 请求参数新增 `tags`，传入企业标签名称，例如“高新技术企业”“专精特新”“上市公司”“科研机构”“高校”等。
- 后端按企业记录的标签列匹配筛选。
- 返回 `total` 必须是筛选后的总数，`items` 必须是筛选后的分页列表。
- 未传 `tags` 时保持现有逻辑不变。

## 2. ChainTalent 人才列表增加籍贯筛选

场景：产业链节点人才列表、链上人才列表后续需要按人才籍贯进一步筛选。

接口范围：
- `GET /api/chain-talents/search`

需求：
- 请求参数新增 `nativePlace`，传入籍贯关键词，例如“湖北”“宜昌”等。
- 后端按人才籍贯字段筛选。
- 返回 `total` 必须是筛选后的总数，`items` 必须是筛选后的分页列表。
- 未传 `nativePlace` 时保持现有逻辑不变。

## 3. 人才图谱关系节点补充

场景：人才菜单搜索到专家后，需要在人才图谱中按关系类型展示和筛选，包括专家合作、专家关联企业、专家关联技术、专家关联产业链根节点、专家所属产业链。

现状：当前 `GET /api/talents/{id}/graph` 只返回 `PERSON`、`ORG` 两类节点，以及 `AFFILIATED`、`COOPERATE` 两类关系。前端可以临时用合作人才、合作机构、关键词和产业链关键词做推断展示，但技术节点和产业链节点不是后端明确关系。

需求：
- 希望新增或增强人才图谱接口，按人才 ID 返回统一关系图数据。
- 节点类型建议至少包含：`PERSON`、`ORG`、`TECH`、`CHAIN_ROOT`、`CHAIN`。
- 关系类型建议至少包含：`COAUTHOR`、`AFFILIATED`、`COOPERATE`、`RELATED_TECH`、`RELATED_CHAIN_ROOT`、`BELONG_CHAIN`。
- 每个节点建议返回 `id`、`name`、`type`、`org/region`、`count/weight` 等展示字段。
- 支持按关系类型筛选，或一次性返回全部关系由前端筛选。
- 如 `ORG` 中包含高校、科研院所、企业，建议返回 `orgType`，便于前端区分“关联企业”和“关联机构”。

# 产业图谱父子关系树说明

## 给后端的文件

推荐发送：`产业图谱父子关系树.json`

## 数据口径

这份 JSON 只表达产业图谱的父子层级关系，不包含统计值、前端状态或展示分区。

已移除：

- `chainKey`
- `id`
- `status`
- `上游 / 中游 / 下游`
- `enterprises`
- `talents`
- `localEnterprises`
- `localTalents`

后端只需要根据节点名称和父子关系做统计、匹配和实时计算。

## 结构

结构参考 `src/data/industry-keywords.json` 的处理方式，直接用中文名称逐层嵌套。  
这里已经去掉 `上游 / 中游 / 下游` 展示分组，后端只需要按父子关系识别节点层级。

```json
{
  "大数据和人工智能": {
    "人工智能": {
      "基础硬件与算力基础设施": {
        "计算与加速芯片": {
          "图形处理器 GPU": {},
          "专用AI加速器 ASIC": {}
        }
      }
    }
  }
}
```

层级含义：

```text
一级产业链
  二级产业链
    图谱根节点
      子节点
        叶子节点
```

空对象 `{}` 表示叶子节点。

## 当前包含范围

当前 JSON 包含已接入图谱的 5 个一级产业链、6 条二级产业链、716 个图谱节点。

已接入二级产业链：

| 一级产业链 | 二级产业链 |
| --- | --- |
| 绿色化工 | 湿电子化学品 |
| 新能源新材料 | 新能源电池 |
| 生命健康 | 先进制剂与高端仿制药 |
| 生命健康 | 酵母发酵与功能成分制造 |
| 汽车及装备制造 | 绿色智能船舶（内河绿色智能船舶制造） |
| 大数据和人工智能 | 人工智能 |

文化旅游目前没有接入图谱，所以不在这份 JSON 中。
